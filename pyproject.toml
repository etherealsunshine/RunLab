[project]
name = "runlab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncio>=4.0.0",
    "modal>=1.3.0.post1",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]
